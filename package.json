{
  "name": "pixels",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.13.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app:dev": "pnpm --filter pixels-app dev",
    "app:tauri": "pnpm --filter pixels-app tauri",
    "server:dev": "pnpm --filter pixels-server start"
  }
}